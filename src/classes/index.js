//Archivo para importar todas las clases y no se vea desordenado en el archivo principal index. js

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';

export {
    Todo,
    TodoList
}